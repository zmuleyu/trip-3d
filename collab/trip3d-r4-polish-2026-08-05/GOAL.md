# GOAL — R4 交互/视觉整体打磨(导入后效果 + 地图显示)

## 最终目标
1. **bug 修复**:列表重影/重复渲染(先 DOM 诊断复现)、版权条与面板按钮重叠、嵌套滚动条收敛
2. **吸附 profile 选择**:步行/驾车(FOSSGIS routed-foot/routed-car);缓存 key 含 profile;摘要卡口径标签(步行/驾车/示意)
3. **长线路视野 fit**:按途经点跨度自适应 demZoom;加载后相机 fit 到界内线路段;超界 toast(已有)
4. **视觉重点**:摘要卡层级(口径修正后大号时长才合理);按钮主次统一;搜索结果导入后自动收起;底图默认观感(vignette/sepia 适度收敛)

## 非目标
多尺度 DEM 金字塔(架构项,followups);多日分段;触控专项

## 验收
- 导入乌兰哈达链接:DOM 无重复、面板单滚动、版权不压按钮、profile=驾车时长大号合理(对比 amap 18h44m 同量级)
- 长环线:相机 fit 后界内线路可见;吸附 2 万点不卡(采样 240 已有)
- 测试/build ✓;review 处理;closeout+发布
